name: Deploy Face Service to Cloud Run

on:
  push:
    branches: [main]
    paths:
      - 'services/face_service/**'
      - '.github/workflows/deploy-face-service.yml'
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Deploy to Cloud Run
        working-directory: services/face_service
        run: |
          gcloud run deploy moodscript-face-service \
            --source . \
            --region us-central1 \
            --project "${{ secrets.GCP_PROJECT_ID }}" \
            --memory 2Gi \
            --update-env-vars "INTERNAL_API_KEY=${{ secrets.INTERNAL_API_KEY }}" \
            --allow-unauthenticated \
            --quiet
